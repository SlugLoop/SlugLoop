import React from 'react'

const AppContext = React.createContext() //create context API needed for passing or changing in data 
//we need this for manipulating how the app looks basically.

export default AppContext
//export so all files has access to it. 